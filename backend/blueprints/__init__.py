"""Flask blueprints for the Digipelago API.

Each module defines a `bp` Blueprint plus every route in the frozen route
contract (auth, themes, connections, telemetry), registered under /api by the
app factory.
"""
